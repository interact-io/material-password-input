import { MatPasswordDirective } from './mat-password.directive';

describe('MatPasswordDirective', () => {
    it('should create an instance', () => {
        const directive = new MatPasswordDirective(null, null, null, null);
        expect(directive).toBeTruthy();
    });
});
